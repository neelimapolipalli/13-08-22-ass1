str=input('Enter a string which contain both upper and lower case letters:')
n=len(str)
count=0
for i in range(0,n):
    if str[i]==' ':
        count=count+1
s=n-count
print("the number of characters in string",s)
print(str.upper())
print(str.lower())
print(str.swapcase())

